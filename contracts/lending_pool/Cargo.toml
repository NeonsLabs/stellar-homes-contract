[package]
name = "sh-lending-pool"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Investor capital for the mortgage pool: deposits, reservations and yield."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
